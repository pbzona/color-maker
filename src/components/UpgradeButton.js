import styles from '../css/UpgradeButton.module.css';

const UpgradeButton = () => {
  return (
    <button className={`${styles.btn} upgrade-btn`}>
      Upgrade Account
    </button>
  )
}

export default UpgradeButton
